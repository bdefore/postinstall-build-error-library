import React from 'react';

const VideoPlayer = React.createClass({
  render() {
    return (
      <div>
        <div>Render Test</div>
      </div>
    );
  }
});

export {Styles} from '../less/Player.less';

export default VideoPlayer;
